# 📱 Налаштування Telegram Login для адмін-панелі

## ❓ Чому я не бачу кнопку Telegram на localhost?

**Коротка відповідь:** Telegram Login Widget працює **ТІЛЬКИ на публічних доменах**, не на localhost.

---

## 🔍 Детальне пояснення

### Обмеження Telegram Login Widget

Telegram Login Widget має жорсткі обмеження безпеки:

1. ✅ **Працює на:** 
   - Публічних доменах (example.com, yourdomain.com)
   - HTTPS сайтах
   - Доменах, зареєстрованих в BotFather

2. ❌ **НЕ працює на:**
   - localhost
   - 127.0.0.1
   - IP адресах (192.168.x.x)
   - Локальних доменах без HTTPS

### Чому так?

Telegram перевіряє:
- Чи домен збігається з тим, що вказано в BotFather
- Чи запит йде з того самого домену
- Чи використовується HTTPS (для продакшн)

---

## 🚀 Що потрібно зробити

### Крок 1: Створити Telegram бота

1. Відкрийте [@BotFather](https://t.me/BotFather) в Telegram
2. Надішліть команду: `/newbot`
3. Дайте боту ім'я: `Novobud Chat Bot` (або інше)
4. Дайте боту username: `novobudchatbot` (має закінчуватись на `bot`)
5. Збережіть токен (він не потрібен для Login Widget, але може знадобитись пізніше)

**Ваш бот:** [@novobudchatbot](https://t.me/novobudchatbot)

### Крок 2: Налаштувати домен в BotFather

**ВАЖЛИВО:** Це потрібно зробити ПІСЛЯ деплою на реальний домен!

1. У BotFather надішліть: `/setdomain`
2. Виберіть вашого бота зі списку
3. Вкажіть ваш домен БЕЗ `https://` та БЕЗ `/`:
   - ✅ Правильно: `kovcheg.com`
   - ✅ Правильно: `admin.kovcheg.com`
   - ❌ Неправильно: `https://kovcheg.com`
   - ❌ Неправильно: `kovcheg.com/admin`

### Крок 3: Оновити код (вже зроблено)

У файлі `/pages/admin/index.tsx` вже є правильний код:

```html
<script
  async
  src="https://telegram.org/js/telegram-widget.js?22"
  data-telegram-login="novobudchatbot"  <!-- Ваш бот -->
  data-size="large"
  data-onauth="onTelegramAuth(user)"
  data-request-access="write"
/>
```

**Поточний бот:** `novobudchatbot`

### Крок 4: Задеплоїти на реальний домен

Після деплою на Vercel, Netlify, або інший хостинг:

1. Перейдіть на ваш домен (наприклад, `https://kovcheg.com/admin`)
2. Тепер кнопка Telegram з'явиться автоматично
3. При натисканні відкриється Telegram для авторизації

---

## 🧪 Як тестувати на localhost?

### Варіант 1: Використовуйте логін/пароль (рекомендовано)

На localhost завжди працює звичайний вхід через email та пароль:

1. Перейдіть на `http://localhost:3002/admin`
2. Виберіть вкладку "Логін і пароль"
3. Введіть email та пароль з бази даних
4. Увійдіть

**Приклад користувача з паролем:**
```javascript
{
  login: "lilkakish@gmail.com",
  password: "$2b$12$N8DkiK/rSI19xNUv7IOR5erHOWBtp8SuoqzhnxK5SRPKRVs8JVoym",
  // Пароль у відкритому вигляді (для тесту): "your_password"
}
```

### Варіант 2: Використовуйте ngrok (для тестування Telegram)

Якщо дуже потрібно протестувати Telegram на localhost:

1. Встановіть ngrok: https://ngrok.com/
2. Запустіть: `ngrok http 3002`
3. Отримаєте публічний URL: `https://abc123.ngrok.io`
4. Налаштуйте цей домен в BotFather: `/setdomain` → `abc123.ngrok.io`
5. Відкрийте `https://abc123.ngrok.io/admin`
6. Тепер кнопка Telegram працюватиме

**Увага:** ngrok URL змінюється при кожному перезапуску (у безкоштовній версії).

---

## ✅ Чеклист перед деплоєм

- [ ] Створено Telegram бота через @BotFather
- [ ] Отримано username бота (наприклад, `kovcheg_estate_bot`)
- [ ] Оновлено `data-telegram-login` в `/pages/admin/index.tsx`
- [ ] Задеплоєно на реальний домен
- [ ] Налаштовано домен в BotFather через `/setdomain`
- [ ] Перевірено, що кнопка з'являється на продакшн домені
- [ ] Протестовано вхід через Telegram

---

## 🎯 Після деплою

### Що станеться:

1. **На сторінці `/admin` з'явиться кнопка:**
   ```
   ┌──────────────────────────┐
   │  🔵 Log in with Telegram │
   └──────────────────────────┘
   ```

2. **При натисканні:**
   - Відкриється Telegram (додаток або веб)
   - Запит на авторизацію від вашого бота
   - Після підтвердження - автоматичний вхід

3. **Система перевірить:**
   - Чи є `telegram_id` в базі даних
   - Чи роль = `admin` або `agent`
   - Чи `is_verified = true`

4. **При успіху:**
   - Створюється токен
   - Зберігається в localStorage
   - Перенаправлення на `/admin/dashboard`

---

## 🐛 Проблеми та рішення

### Кнопка не з'являється на продакшн

**Причини:**
1. Неправильний username бота в коді
2. Домен не налаштований в BotFather
3. Скрипт не завантажився

**Рішення:**
1. Перевірте консоль браузера (F12) → вкладка Console
2. Перевірте вкладку Network → шукайте `telegram-widget.js`
3. Перевірте що username бота правильний
4. Перевірте що домен налаштований: `/setdomain` в @BotFather

### Помилка 405 (Method Not Allowed) при вході

**Причина:** API endpoint не обробляє запит

**Рішення:** Дивіться детальну інструкцію в [TELEGRAM_AUTH_DEBUG.md](./TELEGRAM_AUTH_DEBUG.md)

### Помилка "Telegram акаунт не знайдено"

**Причина:** У базі даних немає користувача з таким `telegram_id`

**Рішення:** Додайте користувача в MongoDB:

```javascript
db.admins.insertOne({
  first_name: "Ваше ім'я",
  last_name: "Ваше прізвище",
  email: "your@email.com",
  login: "your@email.com",
  password: null,  // Без пароля, тільки Telegram
  telegram_id: 123456789,  // Ваш Telegram ID
  role: "admin",
  is_verified: true,
  language_code: "uk",
  created_at: new Date(),
  updated_at: new Date()
})
```

### Як дізнатись свій Telegram ID?

Використайте бота [@userinfobot](https://t.me/userinfobot):
1. Відкрийте бота
2. Натисніть Start
3. Він покаже ваш ID

---

## 📊 Порівняння методів входу

| Функція | Логін/Пароль | Telegram |
|---------|-------------|----------|
| Працює на localhost | ✅ Так | ❌ Ні |
| Працює на продакшн | ✅ Так | ✅ Так |
| Потрібен пароль | ✅ Так | ❌ Ні |
| Потрібен Telegram | ❌ Ні | ✅ Так |
| Швидкість входу | Середня | Швидка |
| Безпека | Висока (bcrypt) | Дуже висока |

---

## 💡 Рекомендації

### Для розробки (localhost):
- Використовуйте логін/пароль
- Створіть тестового користувача з паролем

### Для продакшн:
- Налаштуйте обидва методи
- Telegram для швидкого входу
- Логін/пароль як резервний варіант

### Для команди:
- Адміни можуть мати обидва методи
- Агенти можуть мати тільки Telegram (простіше)

---

## 📞 Підтримка

Якщо виникли проблеми:
1. Перевірте що ви на продакшн домені (не localhost)
2. Перевірте консоль браузера (F12)
3. Перевірте налаштування в @BotFather
4. Перевірте що користувач є в базі даних

---

**Версія:** 1.0  
**Останнє оновлення:** Листопад 2025

